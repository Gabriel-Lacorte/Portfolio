---
import Layout from "@layouts/Layout.astro";
import Dither from "@components/ascii/Dither.astro";
import Ornament from "@components/ascii/Ornament.astro";
import Panel from "@components/ascii/Panel.astro";
import Spec from "@components/ascii/Spec.astro";
import Waterfall from "@components/ascii/Waterfall.astro";
import { postsFor } from "@lib/posts";
import { localised, useTranslations, type Locale } from "@i18n/utils";
import {
    CHANGELOG,
    NOW,
    PROJECTS,
    SITE,
    SOCIALS,
    STACK_GROUPS,
} from "@consts";
import { getSelfTest, getStats, readingTime } from "@lib/stats";

type Props = { locale: Locale };
const { locale } = Astro.props;
const t = useTranslations(locale);
const L = (path: string) => localised(path, locale);

const posts = await postsFor(locale);
const stats = await getStats();
const selftest = getSelfTest(stats);

const iso = (d: Date) => d.toISOString().slice(0, 10);
---

<Layout
    locale={locale}
    title={SITE.TITLE}
    description={t("meta.homeDescription")}
    suffix={false}
>
    <div class="stack-gap">
        <section class="masthead panel">
            <div class="panel-body">
                <Ornament />
                <div class="ident">
                    <h1>
                        <span class="mark">{SITE.HANDLE}</span>
                        <span class="who">{SITE.TITLE} — {t("site.role")}</span>
                    </h1>
                    <p class="statement">{t("site.statement")}</p>
                </div>
            </div>
            <Dither />
        </section>

        <Panel
            title={t("panel.transmissions")}
            note={`${posts.length} ${t("unit.logged")}`}
            bodyClass="flush"
            class="lead"
        >
            <ol class="log">
                {
                    posts.map((post) => (
                        <li>
                            <a href={L(`/blog/${post.data.translationKey}`)}>
                                <time
                                    class="stamp"
                                    datetime={iso(post.data.date)}
                                >
                                    {iso(post.data.date)}
                                </time>
                                <span class="name">{post.data.title}</span>
                                <span class="lead" aria-hidden="true" />
                                <span class="len">
                                    {readingTime(post.body)} {t("unit.min")}
                                </span>
                            </a>
                            <p class="sub">
                                {post.data.description}
                                {post.data.tags.length > 0 && (
                                    <span class="tags">
                                        {post.data.tags.map((tag) => (
                                            <span class="tag">{tag}</span>
                                        ))}
                                    </span>
                                )}
                            </p>
                        </li>
                    ))
                }
            </ol>
        </Panel>

        <div class="grid-2">
            <Panel title={t("panel.builds")} note={`${PROJECTS.length} ${t("unit.listed")}`}>
                <ul class="builds">
                    {
                        PROJECTS.map((project) => (
                            <li>
                                <a href={project.HREF.startsWith("/") ? L(project.HREF) : project.HREF}>{project.NAME}</a>
                                <p class="sub">{project.SUMMARY}</p>
                                <p class="kit">{project.STACK.join(" · ")}</p>
                            </li>
                        ))
                    }
                </ul>
                <a class="more" href={L("/projects")}>{t("action.allBuilds")} ▸</a>
            </Panel>

            {/* Two shorter panels stacked, so the column closes level with
                the taller one beside it instead of trailing dead space. */}
            <div class="stack-gap">
                <Panel title={t("panel.signal")} note={NOW.UPDATED}>
                    <ul class="signal">
                        {
                            NOW.LINES.map((line, i) => (
                                <li>
                                    <span class="idx" aria-hidden="true">
                                        {String(i + 1).padStart(2, "0")}
                                    </span>
                                    <span>{line}</span>
                                </li>
                            ))
                        }
                    </ul>
                    <a class="more" href={L("/now")}>{t("action.fullSignal")} ▸</a>
                </Panel>

                <Panel title={t("panel.log")} note={`${CHANGELOG.length} ${t("unit.entries")}`}>
                    <Spec
                        rows={CHANGELOG.map(({ DATE, TEXT }) => ({
                            k: DATE,
                            v: TEXT,
                            dim: true,
                        }))}
                        class="logspec"
                    />
                </Panel>
            </div>
        </div>

        <div class="grid-2">
            <Panel title={t("panel.operator")} note={t("value.active")}>
                <Spec
                    rows={[
                        { k: t("field.name"), v: SITE.TITLE },
                        { k: t("field.role"), v: t("site.role") },
                        { k: t("field.based"), v: SITE.LOCATION },
                        { k: t("field.writingAbout"), v: t("value.systemsCompilers") },
                        { k: t("field.logged"), v: `${posts.length} ${t("unit.posts")}` },
                        ...SOCIALS.map(({ NAME, HANDLE, HREF }) => ({
                            k: NAME,
                            v: HANDLE,
                            href: HREF,
                            external: true,
                        })),
                        { k: t("field.mail"), v: SITE.EMAIL, href: `mailto:${SITE.EMAIL}` },
                    ]}
                />
            </Panel>

            <Panel title={t("panel.stack")} note={t("value.whatIUse")}>
                <ul class="stack-groups">
                    {
                        STACK_GROUPS.map(({ GROUP, ITEMS }) => (
                            <li>
                                <h3>{GROUP}</h3>
                                <p>{ITEMS.join(" · ")}</p>
                            </li>
                        ))
                    }
                </ul>
            </Panel>
        </div>

        {/* Last, not first: it is a curiosity about how the site is built,
            not a headline. Every line is a fact from this build. */}
        <Panel title={t("panel.selfTest")} note={stats.built}>
            <ul class="spec selftest">
                {
                    selftest.map(({ k, v, s }) => (
                        <li>
                            <span class="k">{k}</span>
                            <span class="lead" aria-hidden="true" />
                            <span class="val">{v}</span>
                            <span class="tri" aria-hidden="true">▲▲▲</span>
                            <span class="v" data-tone="ok">{s}</span>
                        </li>
                    ))
                }
            </ul>
        </Panel>
    </div>
</Layout>

<style>
    .masthead {
        overflow: hidden;
        container-type: inline-size;
    }

    .ident {
        display: flex;
        flex-wrap: wrap;
        align-items: baseline;
        gap: 0.4rem 3ch;
        margin-top: 1rem;
    }

    h1 {
        display: flex;
        flex-wrap: wrap;
        align-items: baseline;
        gap: 0 2ch;
        font-weight: 400;
    }

    /* The identity is a header element, not the page. The writing below is
       meant to be the loudest thing here. */
    .mark {
        font-family: var(--font-display);
        font-size: clamp(15px, 1.9vw, 22px);
        letter-spacing: 0.1em;
        text-transform: uppercase;
        color: var(--g2);
    }

    .who {
        font-size: 0.93em;
        letter-spacing: 0.04em;
        text-transform: uppercase;
        color: var(--dim);
    }

    :global(.panel.lead > .panel-head) {
        font-size: 13.5px;
        padding-block: 0.3rem;
    }

    .stack-groups > li + li {
        margin-top: 0.65rem;
        padding-top: 0.65rem;
        border-top: 1px solid var(--rule);
    }

    .stack-groups h3 {
        font-family: var(--font-display);
        font-size: 11.5px;
        font-weight: 400;
        letter-spacing: 0.16em;
        text-transform: uppercase;
        color: var(--g2);
    }

    .stack-groups p {
        margin-top: 0.1rem;
        color: var(--ink);
    }

    .crossbar {
        margin-top: 0.4rem;
    }

    /* Self-test rows carry a value AND a status, so the leader sits
       between the label and the value and the status pins to the end. */
    :global(.spec.selftest .val) {
        flex: none;
        color: var(--dim);
        text-transform: uppercase;
        letter-spacing: 0.03em;
    }

    :global(.spec.selftest .tri) {
        flex: none;
        color: var(--ok);
        letter-spacing: -0.05em;
        user-select: none;
    }

    /* Fills the panel rather than floating mid-column. */
    .band {
        width: 100%;
        margin-top: var(--step);
        padding-top: 0.6rem;
        border-top: 1px solid var(--rule);
        container-type: inline-size;
        overflow: hidden;
    }

    .crossbar {
        margin-top: 0.9rem;
    }

    .statement {
        flex: 1 1 40ch;
        max-width: var(--measure);
        margin: 0;
        color: var(--ink);
    }

    /* ── Transmissions: a directory listing ───────────────────────── */
    :global(.panel-body.flush) {
        padding-inline: 0;
        padding-block: 0;
    }

    .log > li {
        padding: 0.5rem var(--pad);
    }

    .log > li + li {
        border-top: 1px solid var(--rule);
    }

    .log a {
        display: flex;
        align-items: baseline;
        gap: 1ch;
    }

    .log .stamp {
        flex: none;
        color: var(--dim);
        font-variant-numeric: tabular-nums;
    }

    .log .name {
        flex: none;
        color: var(--bright);
        text-transform: uppercase;
        letter-spacing: 0.04em;
    }

    .log .lead {
        flex: 1;
        min-width: 2ch;
        height: 1px;
        align-self: center;
        background-image: radial-gradient(
            circle at center,
            var(--dot) 0.5px,
            transparent 0.6px
        );
        background-size: 1ch 2px;
        background-repeat: repeat-x;
    }

    .log a:hover .lead {
        opacity: 0;
    }

    .log .len {
        flex: none;
        color: var(--dim);
        font-variant-numeric: tabular-nums;
    }

    .log a:hover .stamp,
    .log a:hover .len,
    .log a:hover .name {
        color: inherit;
    }

    .sub {
        margin-top: 0.1rem;
        color: var(--dim);
        font-size: 0.93em;
    }

    .log .sub {
        padding-left: 12ch;
    }

    .tags {
        display: inline-flex;
        flex-wrap: wrap;
        gap: 0.3rem 1ch;
        margin-left: 1ch;
        vertical-align: middle;
    }

    /* ── Builds ───────────────────────────────────────────────────── */
    .builds > li + li {
        margin-top: 0.7rem;
        padding-top: 0.7rem;
        border-top: 1px solid var(--rule);
    }

    .builds a {
        color: var(--bright);
        text-transform: uppercase;
        letter-spacing: 0.06em;
    }

    .builds a::before {
        content: "▸ ";
        color: var(--dim);
    }

    .kit {
        margin-top: 0.15rem;
        color: var(--dot);
        font-size: 0.88em;
        text-transform: uppercase;
        letter-spacing: 0.04em;
    }

    /* ── Signal ───────────────────────────────────────────────────── */
    .signal > li {
        display: flex;
        gap: 1.5ch;
        color: var(--dim);
    }

    .signal > li + li {
        margin-top: 0.4rem;
    }

    .signal .idx {
        flex: none;
        color: var(--dot);
        font-variant-numeric: tabular-nums;
    }

    .more {
        display: inline-block;
        margin-top: 0.9rem;
        color: var(--dim);
        font-size: 0.9em;
        text-transform: uppercase;
        letter-spacing: 0.08em;
    }

    /* The log reads as prose and now sits in a narrow column, so its rows
       stack: date above, entry below, leader dropped. */
    :global(.logspec > li) {
        display: block;
    }

    :global(.logspec > li + li) {
        margin-top: 0.35rem;
    }

    :global(.logspec .lead) {
        display: none;
    }

    :global(.logspec .k) {
        font-variant-numeric: tabular-nums;
        color: var(--dot);
    }

    :global(.logspec .v) {
        display: block;
        text-align: left;
        text-transform: none;
        letter-spacing: 0;
        color: var(--dim);
    }

    @media (max-width: 680px) {
        .log a {
            flex-wrap: wrap;
        }

        .log .lead {
            display: none;
        }

        .log .sub {
            padding-left: 0;
        }
    }
</style>
