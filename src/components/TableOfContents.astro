---
import TableOfContentsHeading from "./TableOfContentsHeading.astro";

export interface Heading {
    depth: number;
    slug: string;
    text: string;
    subheadings: Heading[];
}

type Props = {
    headings: Omit<Heading, "subheadings">[];
    /** Localised label for the summary. Ignored when `bare`. */
    label?: string;
    /** Drop the disclosure wrapper, for when a panel already titles it,
     *  which otherwise renders "contents" twice, one inside the other. */
    bare?: boolean;
    /** The headings' own language, when it is not the page's. Goes on the
     *  list rather than the wrapper so `label` stays in the page's. */
    lang?: string;
};

const { headings, label = "contents", bare = false, lang } = Astro.props;

/* h2/h3 only. An h3 with no h2 above it is promoted rather than dropped,
   which a parent lookup would throw on. */
function buildToc(flat: Omit<Heading, "subheadings">[]): Heading[] {
    const toc: Heading[] = [];

    for (const raw of flat) {
        if (raw.depth > 3) continue;

        const heading: Heading = { ...raw, subheadings: [] };

        if (heading.depth <= 2 || toc.length === 0) {
            toc.push(heading);
        } else {
            toc[toc.length - 1].subheadings.push(heading);
        }
    }

    return toc;
}

const toc = buildToc(headings);
---

{
    toc.length > 0 &&
        (bare ? (
            <nav class="toc-bare" aria-label={label}>
                <ul lang={lang}>
                    {toc.map((heading) => (
                        <TableOfContentsHeading heading={heading} />
                    ))}
                </ul>
            </nav>
        ) : (
            <details class="toc" open>
                <summary>{label}</summary>
                <nav aria-label={label}>
                    <ul lang={lang}>
                        {toc.map((heading) => (
                            <TableOfContentsHeading heading={heading} />
                        ))}
                    </ul>
                </nav>
            </details>
        ))
}

<style>
    .toc {
        width: fit-content;
        max-width: 100%;
        margin-bottom: 15px;
        border-left: var(--hair);
        padding: 6px 10px;
    }

    summary {
        cursor: pointer;
        list-style: none;
        color: var(--comment);
    }

    summary::-webkit-details-marker {
        display: none;
    }

    summary::before {
        content: "[+] " / "";
    }

    .toc[open] summary::before {
        content: "[-] " / "";
    }

    summary:hover {
        color: var(--bg);
        background: var(--base);
    }

    nav :global(ul) {
        margin: 0;
        padding: 0;
        list-style: none;
    }

    nav > ul {
        margin-top: 6px;
    }

    .toc-bare > ul {
        columns: 2;
        column-gap: 24px;
    }

    @media (max-width: 700px) {
        .toc-bare > ul {
            columns: 1;
        }
    }
</style>
